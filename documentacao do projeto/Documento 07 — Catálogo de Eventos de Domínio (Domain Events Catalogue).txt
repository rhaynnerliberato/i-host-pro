iHostPro
Documento 07
Catálogo de Eventos de Domínio (Domain Events Catalogue)

Versão: 1.0

Status: Oficial

1. Objetivo

Este documento define todos os eventos oficiais do domínio do iHostPro.

Os eventos representam fatos ocorridos dentro da plataforma e constituem o principal mecanismo de comunicação entre módulos.

Todo comportamento automatizado deverá ser iniciado pela publicação de um ou mais eventos de domínio.

Nenhum módulo deverá executar lógica de negócio pertencente a outro módulo através de chamadas diretas quando existir um evento equivalente.

2. Princípios

Todo evento representa um fato consumado.

Um evento:

nunca representa uma intenção;
nunca representa um comando;
nunca representa uma solicitação.

Eventos representam apenas algo que realmente aconteceu.

Exemplos corretos:

ReservaConfirmada
FaxinaConcluida
PixRecebido

Exemplos incorretos:

CriarFaxina
EnviarSenha
AtualizarAgenda

Esses são comandos, não eventos.

3. Estrutura Padrão de um Evento

Todo evento deverá possuir obrigatoriamente os seguintes campos:

Campo	Obrigatório	Descrição
EventId	Sim	Identificador único
EventName	Sim	Nome oficial do evento
AggregateId	Sim	Identificador da entidade principal
AggregateType	Sim	Tipo da entidade
TenantId	Sim	Cliente proprietário
Timestamp	Sim	Data e hora UTC
Version	Sim	Versão do evento
CorrelationId	Sim	Identificador da operação
CausationId	Sim	Evento que originou este
ActorType	Sim	Usuário, IA, Sistema, Integração
ActorId	Opcional	Identificador do ator
Payload	Sim	Dados do evento
4. Convenção de Nomenclatura

Todos os eventos deverão utilizar:

Verbo no passado.

Exemplos:

ReservaCriada

ReservaConfirmada

ReservaCancelada

FaxinaCriada

FaxinaConcluida

MensagemRecebida

PixConfirmado

Nunca utilizar:

CriarReserva

EnviarMensagem

AtualizarAgenda

5. Eventos do Módulo Reservas
ReservationCreated

Publicado quando uma nova reserva é criada.

Consumidores típicos:

Agenda
Faxinas
Dashboard
Auditoria
IA
ReservationUpdated

Quando qualquer dado relevante da reserva for alterado.

ReservationCancelled

Quando a reserva for cancelada.

Dispara:

cancelamento da faxina;
atualização da agenda;
notificações.
ReservationConfirmed

Reserva validada.

GuestCheckedIn

Check-in realizado.

GuestCheckedOut

Checkout realizado.

ReservationClosed

Reserva totalmente encerrada.

6. Eventos do Módulo Faxinas
CleaningCreated

Faxina criada.

CleaningAssigned

Faxineira definida.

CleaningStarted

Faxina iniciada.

CleaningInspectionStarted

Inspeção iniciada.

CleaningCompleted

Faxina concluída.

Consumidores:

IA

Agenda

Dashboard

Portaria

CleaningCancelled

Faxina cancelada.

CleaningDelayed

Faxina atrasada.

CleaningNeedsHelp

Solicitou ajuda.

CleaningNeedsMaterial

Solicitou materiais.

7. Eventos do Hóspede
GuestMessageReceived

Mensagem recebida.

GuestQuestionAnswered

Pergunta respondida.

GuestRequestedEarlyCheckin

Solicitação criada.

GuestRequestedLateCheckout

Solicitação criada.

GuestReportedProblem

Problema informado.

GuestRequestedHuman

Solicitou administrador.

8. Eventos do Agente IA
AIConversationStarted
AIConversationEnded
AIEscalatedToHuman
AIExecutedWorkflow
AIDecisionMade
AIMessageSent
AIUnableToAnswer
9. Eventos Financeiros
PixPaymentReceived
PixPaymentConfirmed
PaymentFailed
RefundRequested
RefundApproved
RefundRejected

Nota (Fase 10, Checkpoint 5): `PixPaymentReceived`/`PixPaymentConfirmed`, listados acima apenas como nomes prospectivos (sem payload, sem contexto de publicação), foram SUPERSEDIDOS pelo contrato real implementado — ver §41 (`PixChargeCreated`/`PixChargeConfirmed`, `Payments.Contracts`). Os dois nomes acima nunca serão materializados como eventos distintos. `PaymentFailed`/`RefundRequested`/`RefundApproved`/`RefundRejected` permanecem prospectivos, sem mudança — ver §41 para o registro completo desta decisão.
10. Eventos da Comunicação
MessageQueued
MessageSending
MessageSent
MessageDelivered
MessageRead
MessageFailed
MessageRetried
11. Eventos da Agenda
ScheduleUpdated
CalendarBlocked
CalendarReleased
EarlyCheckinApproved
LateCheckoutApproved
LateCheckoutDenied
12. Eventos das Configurações
PropertyConfigurationUpdated
PolicyUpdated
TemplateUpdated
IntegrationConfigured
AIConfigurationUpdated
13. Eventos de Usuários
UserCreated
UserUpdated
UserBlocked
UserUnblocked
UserRoleAssigned
UserRoleRemoved
UserLoggedIn
UserLoggedOut
PasswordChanged

Login rejeitado (qualquer motivo interno: tenant não encontrado/inativo, usuário não encontrado, usuário bloqueado, senha incorreta).

LoginFailed

Conta bloqueada por número de tentativas de login inválidas.

AccountLockedOut

Reutilização de um refresh token já rotacionado detectada (fora da janela de graça de concorrência).

RefreshTokenReuseDetected

Sessão revogada (logout, reuse detectado ou revogação administrativa).

SessionRevoked

Consumidores típicos (LoginFailed, AccountLockedOut, RefreshTokenReuseDetected, SessionRevoked):

Auditoria

Adicionados durante o planejamento do Incremento 2 do contexto Identity & Access (login, JWT, refresh token, logout), conforme aprovação registrada em ADR-012.

13.1 Payloads dos Eventos do Incremento 2 (Login, JWT, Refresh Token, Logout)

Implementados na Etapa 15 do Incremento 2 (ver ADR-013 para a convenção de roteamento RabbitMQ). Nenhum destes eventos inclui e-mail, endereço IP, User-Agent, senha, access token, refresh token, hash de token ou qualquer outro segredo — apenas identificadores e códigos estáveis.

Convenção comum a todos os seis, além dos campos obrigatórios do envelope (Seção 3):

- AggregateType/AggregateId representam o Usuário (AggregateId = userId) em todos os seis eventos, exceto LoginFailed, cujo AggregateType/AggregateId representam o Tenant (AggregateId = tenantId) — o único caso em que o usuário pode não existir (ex.: e-mail não cadastrado), enquanto o tenant já foi resolvido antes deste evento poder ser publicado.
- ActorType é sempre "User" (o fato é sempre desencadeado por uma requisição de um cliente, mesmo quando rejeitada ou classificada pelo sistema). ActorId é o userId quando conhecido, ausente quando não (LoginFailed com usuário não encontrado).
- Quando dois eventos são publicados pela mesma operação (LoginFailed+AccountLockedOut; UserLoggedOut+SessionRevoked; RefreshTokenReuseDetected+SessionRevoked), o segundo evento da dupla preenche CausationId com o EventId do primeiro.

UserLoggedIn
| Campo | Tipo | Descrição |
|---|---|---|
| SessionId | Guid | Sessão criada por este login |

LoginFailed
| Campo | Tipo | Descrição |
|---|---|---|
| UserId | Guid? | Nulo quando o e-mail não corresponde a nenhum usuário do tenant |
| ReasonCode | string | Código ASCII estável — ver tabela de códigos abaixo |

Códigos de ReasonCode para LoginFailed:
| Código | Situação |
|---|---|
| user_not_found | Nenhum usuário com este e-mail no tenant |
| user_blocked | Usuário existe, está com status Blocked |
| invalid_password | Senha incorreta — usado tanto quando a tentativa não atinge o limiar de bloqueio quanto na própria tentativa que o atinge (neste segundo caso, acompanhado de AccountLockedOut na mesma operação) |
| account_locked | Conta já estava bloqueada por tentativas anteriores (distinto de AccountLockedOut, que representa a tentativa que efetivamente inicia o bloqueio) |

AccountLockedOut
| Campo | Tipo | Descrição |
|---|---|---|
| LockoutEnd | DateTimeOffset (UTC) | Instante em que o bloqueio expira |
| ReasonCode | string | Código ASCII estável — ver tabela de códigos abaixo |

Códigos de ReasonCode para AccountLockedOut:
| Código | Situação |
|---|---|
| max_failed_attempts | Número de tentativas de senha incorreta atingiu o limiar configurado (único gatilho existente neste incremento) |

UserLoggedOut
| Campo | Tipo | Descrição |
|---|---|---|
| SessionId | Guid | Sessão encerrada por este logout |

RefreshTokenReuseDetected
| Campo | Tipo | Descrição |
|---|---|---|
| SessionId | Guid | Sessão cuja cadeia de rotação foi invalidada |
| TokenId | Guid | Identificador público do refresh token reutilizado (nunca o hash ou o token completo) |

SessionRevoked
| Campo | Tipo | Descrição |
|---|---|---|
| SessionId | Guid | Sessão revogada |
| ReasonCode | string | Código ASCII estável — ver tabela de códigos abaixo |

Códigos de ReasonCode para SessionRevoked:
| Código | Situação | Publicado desde |
|---|---|---|
| logout_requested | Revogação por logout explícito | Incremento 2 |
| refresh_token_reuse_detected | Revogação por reutilização de refresh token já rotacionado, fora da janela de graça | Incremento 2 |
| user_blocked | Revogação em cascata de todas as sessões ativas ao bloquear um usuário | Incremento 3, Checkpoint 7 — publicado por BlockUserCommand (POST /api/v1/users/{userId}/block) |
| roles_changed | Revogação em cascata de todas as sessões ativas ao atribuir ou remover um papel | Incremento 3, Checkpoint 6 — publicado por AssignRoleCommand (POST /api/v1/users/{userId}/roles) e RemoveRoleCommand (DELETE /api/v1/users/{userId}/roles/{roleCode}) |
| password_changed | Revogação em cascata de todas as sessões ativas ao alterar ou redefinir a senha | Incremento 3, Checkpoint 9 — publicado por ChangeOwnPasswordCommand (POST /api/v1/users/me/change-password) e AdminResetPasswordCommand (POST /api/v1/users/{userId}/reset-password) |
| user_requested_revocation | Revogação de uma sessão específica, solicitada pelo próprio usuário | Incremento 3, Checkpoint 4 — publicado por RevokeOwnSessionCommand (DELETE /api/v1/users/me/sessions/{sessionId}) |

13.2 Roteamento RabbitMQ dos Eventos do Incremento 2

Ver ADR-013 (Convenção de Roteamento RabbitMQ para Integration Events) para a decisão completa. Resumo: exchange único por Bounded Context, tipo topic, routing key = nome do evento em snake_case.

| Evento | Exchange | Routing Key |
|---|---|---|
| UserLoggedIn | identity-events | user_logged_in |
| LoginFailed | identity-events | login_failed |
| AccountLockedOut | identity-events | account_locked_out |
| UserLoggedOut | identity-events | user_logged_out |
| RefreshTokenReuseDetected | identity-events | refresh_token_reuse_detected |
| SessionRevoked | identity-events | session_revoked |

13.3 Payloads dos Eventos do Incremento 3, Checkpoint 1 (Gestão de Usuários e Sessões)

Contratos e vocabulário de códigos definidos no Checkpoint 1 do Incremento 3 do contexto Identity & Access (autorização RBAC+ABAC e gestão de usuários/sessões). UserCreated é publicado desde o Checkpoint 5, pelo CreateUserCommand (POST /api/v1/users). UserRoleAssigned é publicado desde o Checkpoint 5 (pelo mesmo CreateUserCommand, para o papel inicial obrigatório) e também desde o Checkpoint 6, pelo AssignRoleCommand (POST /api/v1/users/{userId}/roles). UserRoleRemoved é publicado desde o Checkpoint 6, pelo RemoveRoleCommand (DELETE /api/v1/users/{userId}/roles/{roleCode}). UserBlocked é publicado desde o Checkpoint 7, pelo BlockUserCommand (POST /api/v1/users/{userId}/block); UserUnblocked também desde o Checkpoint 7, pelo UnblockUserCommand (POST /api/v1/users/{userId}/unblock). UserUpdated é publicado desde o Checkpoint 8, pelo UpdateUserCommand (PATCH /api/v1/users/{userId}) — somente quando pelo menos um campo efetivamente muda (Seção 4 da decisão do Checkpoint 8: idempotência). PasswordChanged é publicado desde o Checkpoint 9, pelo ChangeOwnPasswordCommand (POST /api/v1/users/me/change-password, ChangeType = self) e pelo AdminResetPasswordCommand (POST /api/v1/users/{userId}/reset-password, ChangeType = admin_reset). Nenhum destes eventos inclui e-mail, endereço IP, User-Agent, senha, hash de senha, access token, refresh token, hash de token ou qualquer outro segredo — apenas identificadores e códigos estáveis, mesma regra do §13.1.

Convenção comum aos sete:

- AggregateType/AggregateId representam o Usuário afetado (AggregateId = userId) em todos os sete eventos.
- ActorType é sempre "User". ActorId é o id do Administrador que executou a ação — exceto em PasswordChanged com ChangeType = self, em que ActorId é o próprio usuário afetado (o mesmo valor de AggregateId). É por isso que nenhum destes sete eventos possui um campo próprio como CreatedByUserId, BlockedByUserId, AssignedByUserId ou equivalente — ActorId já é o campo padrão do envelope (Seção 3) para "quem executou a ação"; repeti-lo com outro nome em cada evento seria duplicação.
- UserCreated é sempre publicado na mesma transação que um UserRoleAssigned para o papel inicial obrigatório (Seção 5 da decisão do Incremento 3: não é permitido criar usuário sem papel) — o CausationId de UserRoleAssigned preenche o EventId de UserCreated.
- UserBlocked, UserRoleAssigned, UserRoleRemoved e PasswordChanged são sempre publicados na mesma transação que um SessionRevoked por sessão ativa revogada em cascata (ver §13.1) — cada SessionRevoked preenche CausationId com o EventId do evento primário correspondente.
- Nenhum destes eventos é publicado quando a operação não altera estado efetivamente (ex.: atribuir um papel já atribuído, bloquear um usuário já bloqueado) — ver as regras de idempotência aprovadas no planejamento do Incremento 3.

UserCreated
| Campo | Tipo | Descrição |
|---|---|---|
| (nenhum campo próprio) | — | AggregateId já identifica o usuário criado; o papel inicial é informado pelo UserRoleAssigned publicado na mesma transação |

UserUpdated
| Campo | Tipo | Descrição |
|---|---|---|
| ChangedFields | string[] | Códigos estáveis dos campos efetivamente alterados: "full_name", "email" — nunca o valor novo nem o anterior |

UserBlocked
| Campo | Tipo | Descrição |
|---|---|---|
| (nenhum campo próprio) | — | AggregateId já identifica o usuário bloqueado |

UserUnblocked
| Campo | Tipo | Descrição |
|---|---|---|
| (nenhum campo próprio) | — | AggregateId já identifica o usuário desbloqueado; nunca restaura sessões revogadas pelo UserBlocked anterior |

UserRoleAssigned
| Campo | Tipo | Descrição |
|---|---|---|
| RoleCode | string | Código estável do papel (ex.: "ADMIN") — nunca o nome de exibição |

UserRoleRemoved
| Campo | Tipo | Descrição |
|---|---|---|
| RoleCode | string | Código estável do papel removido |

PasswordChanged
| Campo | Tipo | Descrição |
|---|---|---|
| ChangeType | string | Código ASCII estável — ver tabela de códigos abaixo |

Códigos de ChangeType para PasswordChanged:
| Código | Situação |
|---|---|
| self | Alteração pelo próprio usuário, mediante confirmação da senha atual |
| admin_reset | Redefinição por um Administrador, sem exigir a senha atual — o Administrador não pode usar este fluxo para alterar a própria senha |

13.4 Roteamento RabbitMQ dos Eventos do Incremento 3

Convenção já registrada em ADR-013 (exchange único por Bounded Context, tipo topic, routing key = nome do evento em snake_case). O roteamento em Program.cs (IHostPro.Api) só é registrado no checkpoint que implementa o primeiro comando a publicar aquele evento — nunca antecipadamente, para não registrar rota RabbitMQ para um evento que nenhum código ainda emite. UserCreated/UserRoleAssigned já estão registrados desde o Checkpoint 5; UserRoleRemoved desde o Checkpoint 6 (AssignRoleCommand reutiliza a rota já existente de UserRoleAssigned, sem duplicar configuração); UserBlocked/UserUnblocked desde o Checkpoint 7; UserUpdated desde o Checkpoint 8; PasswordChanged desde o Checkpoint 9 (ChangeOwnPasswordCommand e AdminResetPasswordCommand reutilizam a mesma rota, sem duplicar configuração).

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| UserCreated | identity-events | user_created | Checkpoint 5 |
| UserUpdated | identity-events | user_updated | Checkpoint 8 |
| UserBlocked | identity-events | user_blocked | Checkpoint 7 |
| UserUnblocked | identity-events | user_unblocked | Checkpoint 7 |
| UserRoleAssigned | identity-events | user_role_assigned | Checkpoint 5 |
| UserRoleRemoved | identity-events | user_role_removed | Checkpoint 6 |
| PasswordChanged | identity-events | password_changed | Checkpoint 9 |

14. Eventos do Módulo Property Management
CondominiumCreated
CondominiumUpdated
PropertyCreated
PropertyUpdated
PropertyActivated
PropertyDeactivated
PropertyArchived
PropertyOwnerLinked
PropertyOwnerUnlinked

14.1 Payloads dos Eventos do Incremento 1 (Fase 2), Checkpoints 2, 3 e 4 (Condomínios, Imóveis administrativos e Lifecycle de Imóveis)

Implementados nos Checkpoints 2 (Condomínios), 3 (Imóveis administrativos) e 4 (Lifecycle de Imóveis) do Incremento 1 da Fase 2 (Property Management). Nenhum destes sete eventos inclui nome, endereço, CEP, capacidade, código do imóvel, id do condomínio (antigo ou novo), status anterior ou qualquer outro dado de negócio — apenas identificadores e, quando aplicável, códigos estáveis de campo alterado, mesma regra do §13.1/§13.3.

Convenção comum aos sete:

- AggregateType/AggregateId representam o Condomínio (CondominiumCreated/CondominiumUpdated) ou o Imóvel (todos os demais) afetado.
- ActorType é sempre "User". ActorId é o id do Administrador que executou a ação (PROPERTIES:MANAGE).
- CondominiumUpdated/PropertyUpdated nunca são publicados quando a operação não altera estado efetivamente (PATCH idempotente — nenhum campo suprido difere, após normalização, do valor atual) — ver regras de idempotência aprovadas nos Checkpoints 2 e 3.
- PropertyActivated/PropertyDeactivated/PropertyArchived são publicados exatamente uma vez por transição de estado efetivamente confirmada — nunca em uma rejeição (transição inválida, estado já no destino, conflito de concorrência) — ver Checkpoint 4, §3/§4.

CondominiumCreated
| Campo | Tipo | Descrição |
|---|---|---|
| CondominiumId | Guid | Redundante com AggregateId — mantido por explicitação, mesma convenção de CondominiumUpdated |

CondominiumUpdated
| Campo | Tipo | Descrição |
|---|---|---|
| CondominiumId | Guid | Redundante com AggregateId |
| ChangedFields | string[] | Códigos estáveis dos campos efetivamente alterados, na ordem: "name", "address" — nunca o valor novo nem o anterior |

PropertyCreated
| Campo | Tipo | Descrição |
|---|---|---|
| PropertyId | Guid | Redundante com AggregateId |
| Status | string | Código estável do status inicial — sempre "draft" neste checkpoint (todo Imóvel nasce em Draft); nunca o nome bruto do enum |

PropertyUpdated
| Campo | Tipo | Descrição |
|---|---|---|
| PropertyId | Guid | Redundante com AggregateId |
| ChangedFields | string[] | Códigos estáveis dos campos efetivamente alterados, na ordem: "code", "name", "capacity", "condominium_id", "address" — nunca o valor novo nem o anterior |

PropertyActivated
| Campo | Tipo | Descrição |
|---|---|---|
| PropertyId | Guid | Redundante com AggregateId |

PropertyDeactivated
| Campo | Tipo | Descrição |
|---|---|---|
| PropertyId | Guid | Redundante com AggregateId |

PropertyArchived
| Campo | Tipo | Descrição |
|---|---|---|
| PropertyId | Guid | Redundante com AggregateId — Archived é terminal neste incremento, sem evento de restauração |

14.2 Roteamento RabbitMQ dos Eventos do Incremento 1 (Fase 2)

Convenção já registrada em ADR-013 (exchange único por Bounded Context, tipo topic, routing key = nome do evento em snake_case) — mesmo padrão do contexto Identity & Access (§13.2/§13.4), aplicado aqui ao seu próprio exchange, nunca reaproveitando identity-events. O roteamento em Program.cs (IHostPro.Api) só é registrado no checkpoint que implementa o primeiro comando a publicar aquele evento.

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| CondominiumCreated | property-management-events | condominium_created | Checkpoint 2 |
| CondominiumUpdated | property-management-events | condominium_updated | Checkpoint 2 |
| PropertyCreated | property-management-events | property_created | Checkpoint 3 |
| PropertyUpdated | property-management-events | property_updated | Checkpoint 3 |
| PropertyActivated | property-management-events | property_activated | Checkpoint 4 |
| PropertyDeactivated | property-management-events | property_deactivated | Checkpoint 4 |
| PropertyArchived | property-management-events | property_archived | Checkpoint 4 |
| PropertyOwnerLinked | property-management-events | property_owner_linked | Checkpoint 5 |
| PropertyOwnerUnlinked | property-management-events | property_owner_unlinked | Checkpoint 5 |

14.3 Payloads dos Eventos do Checkpoint 5 (Ownership de Imóveis)

Implementados no Checkpoint 5 do Incremento 1 da Fase 2 (Property Management) — vínculo/desvínculo entre um usuário do contexto Identity & Access e um Imóvel. Nenhum destes dois eventos inclui nome, e-mail, papel, status ou qualquer outro dado pessoal do proprietário, nem qualquer dado do Imóvel (código, nome, endereço) — apenas identificadores, mesma regra do §13.1/§13.3/§14.1.

Convenção comum aos dois:

- AggregateType/AggregateId representam o Imóvel afetado (AggregateId = PropertyId), nunca o Owner.
- TenantId (envelope, Seção 3) já identifica o tenant — nenhum campo próprio redundante.
- ActorType é sempre "User". ActorId é o id do Administrador que executou o vínculo/desvínculo (PROPERTIES:MANAGE) — nunca o próprio Owner (este Checkpoint não expõe nenhuma ação de escrita ao Owner).
- Publicados exatamente uma vez por vínculo/desvínculo efetivamente confirmado — nunca em uma rejeição (usuário inexistente/inelegível, Imóvel inexistente, vínculo já existente/inexistente) — ver Checkpoint 5, item 15.
- O vínculo/desvínculo é permitido para um Imóvel em qualquer status (Draft/Active/Inactive/Archived) — estes dois eventos nunca alteram, e nunca dependem de, PropertyStatus.

PropertyOwnerLinked
| Campo | Tipo | Descrição |
|---|---|---|
| PropertyId | Guid | Redundante com AggregateId |
| OwnerUserId | Guid | Identificador opaco do usuário do contexto Identity & Access vinculado como proprietário — nunca nome/e-mail/papel |

PropertyOwnerUnlinked
| Campo | Tipo | Descrição |
|---|---|---|
| PropertyId | Guid | Redundante com AggregateId |
| OwnerUserId | Guid | Identificador opaco do usuário desvinculado |

15. Eventos da Auditoria
AuditRecordCreated
SensitiveOperationExecuted
PermissionChanged
16. Eventos das Integrações
AirbnbSyncStarted
AirbnbReservationImported
AirbnbReservationUpdated
AirbnbReservationCancelled
WhatsAppMessageReceived
WhatsAppMessageSent
WhatsAppWebhookFailed

16.1 Payloads dos Eventos do Checkpoint 3.2 (Fase 9 — Airbnb Deterministic Foundation)

Formalizados em ExternalIntegrations.Contracts (ADR-023). AirbnbSyncStarted é declarado mas deliberadamente não publicado/consumido ainda — sem orquestração de sync real neste checkpoint. Os outros três são publicados por External Integrations e consumidos por Reservations, de forma inteiramente assíncrona (nenhuma nova exceção síncrona, ADR-023). Nenhum dos quatro carrega e-mail, telefone, reviews, conteúdo de mensagens, preço/moeda/taxas ou payload bruto do provider.

AirbnbSyncStarted
| Campo | Tipo | Descrição |
|---|---|---|
| SyncExecutionId | Guid | Identificador da execução de sincronização — sem modelo de orquestração real ainda |
| StartedAtUtc | DateTimeOffset | Momento em que a sincronização começou |

AirbnbReservationImported
| Campo | Tipo | Descrição |
|---|---|---|
| ExternalReservationId | string | Identificador da reserva no Airbnb — chave de idempotência junto com TenantId/Source |
| PropertyId | Guid | Identificador interno já resolvido (ExternalListingId → PropertyId via AirbnbListingMapping) — nunca o listing id externo |
| GuestName | string | Nome do hóspede — único dado pessoal transportado, exigido pelo próprio Reservation.CreateImported |
| CheckInAt | DateTimeOffset | Data/hora de check-in |
| CheckOutAt | DateTimeOffset | Data/hora de check-out |
| GuestCount | int | Quantidade de hóspedes |
| OccurredAtUtc | DateTimeOffset | Momento do fato reportado pela sincronização |

AirbnbReservationUpdated
| Campo | Tipo | Descrição |
|---|---|---|
| ExternalReservationId | string | Identidade externa usada para localizar a Reservation já importada |
| PropertyId | Guid | Snapshot atual (já resolvido), nunca um diff bruto do provider |
| GuestName | string | Snapshot atual |
| CheckInAt | DateTimeOffset | Snapshot atual |
| CheckOutAt | DateTimeOffset | Snapshot atual |
| GuestCount | int | Snapshot atual |
| OccurredAtUtc | DateTimeOffset | Momento do fato reportado pela sincronização |

AirbnbReservationCancelled
| Campo | Tipo | Descrição |
|---|---|---|
| ExternalReservationId | string | Identidade externa usada para localizar a Reservation já importada — nenhum outro dado |
| OccurredAtUtc | DateTimeOffset | Momento do fato reportado pela sincronização |

17. Eventos do Sistema
SystemStarted
JobStarted
JobFinished
JobFailed
BackupCompleted
CacheInvalidated
HealthCheckFailed
18. Cadeias de Eventos
Exemplo 1

Nova Reserva

↓

ReservationCreated

↓

CleaningCreated

↓

ScheduleUpdated

↓

DashboardUpdated

↓

AuditRecordCreated

Exemplo 2

Faxina Concluída

↓

CleaningCompleted

↓

EarlyCheckinAvailable

↓

AIMessageSent

↓

PropertyReleased

↓

DashboardUpdated

Exemplo 3

PIX Recebido

↓

PixPaymentConfirmed

↓

LateCheckoutApproved

↓

CalendarBlocked

↓

CleaningScheduleUpdated

↓

GuestNotified

↓

AuditRecordCreated

19. Eventos Compostos

Um único evento poderá originar diversos outros.

Exemplo:

GuestCheckedOut

↓

CleaningCreated

↓

CleaningAssigned

↓

PropertyInspectionStarted

↓

DashboardUpdated

↓

AuditCreated

Todos deverão possuir CorrelationId comum.

20. Versionamento

Eventos nunca poderão ser alterados após publicação.

Caso seja necessário modificar sua estrutura:

Criar nova versão.

Exemplo:

ReservationCreatedV2

Preservando compatibilidade.

21. Idempotência

Todo consumidor deverá suportar o processamento repetido do mesmo evento.

Processar o mesmo evento duas vezes nunca poderá produzir efeitos duplicados.

22. Garantias

A plataforma deverá garantir:

publicação confiável;
processamento assíncrono;
rastreabilidade;
reprocessamento;
ordenação quando necessária;
isolamento entre consumidores.
23. Eventos Proibidos

Nunca criar eventos que representem:

consultas;
comandos;
intenções;
validações intermediárias.

Eventos representam apenas fatos consumados.

24. Auditoria

Todo evento deverá gerar automaticamente um registo de auditoria contendo:

evento;
data;
origem;
utilizador;
entidade afetada;
resultado;
correlação.
25. Evolução do Catálogo

Novos eventos deverão:

representar um facto de negócio;
possuir consumidores identificados;
ser documentados neste catálogo antes da implementação;
manter compatibilidade com versões anteriores.

Nenhum evento poderá ser introduzido diretamente no código sem atualização deste documento.

26. Catálogo Resumido
Domínio	Eventos Principais
Reservas	ReservationCreated, ReservationConfirmed, ReservationUpdated, ReservationCancelled, GuestCheckedIn, GuestCheckedOut, ReservationClosed
Faxinas	CleaningCreated, CleaningAssigned, CleaningStarted, CleaningCompleted, CleaningCancelled, CleaningDelayed
Comunicação	MessageQueued, MessageSent, MessageDelivered, MessageRead, MessageFailed
IA	AIConversationStarted, AIDecisionMade, AIEscalatedToHuman
Agenda	ScheduleUpdated, CalendarBlocked, CalendarReleased
Financeiro	PixPaymentConfirmed, RefundRequested
Sistema	JobStarted, JobFinished, BackupCompleted
Identity & Access	UserLoggedIn, LoginFailed, AccountLockedOut, UserLoggedOut, RefreshTokenReuseDetected, SessionRevoked, UserCreated, UserUpdated, UserBlocked, UserUnblocked, UserRoleAssigned, UserRoleRemoved, PasswordChanged
Property Management	CondominiumCreated, CondominiumUpdated, PropertyCreated, PropertyUpdated, PropertyActivated, PropertyDeactivated, PropertyArchived, PropertyOwnerLinked, PropertyOwnerUnlinked
Configuration & Policy	PolicyUpdated

27. Eventos do Módulo Reservations — Payloads e Roteamento (Fase 3, Incremento 1)

Complementa a lista prospectiva do §5 ("Eventos do Módulo Reservas") com o payload e roteamento reais dos três eventos já implementados no Incremento 1 da Fase 3 (Reservation Management) — ReservationCreated, ReservationUpdated, ReservationCancelled — mais ReservationClosed, implementado na Fase 10, Checkpoint 1 (ver §27.4). ReservationConfirmed permanece prospectivo, sem payload/roteamento definido. GuestCheckedIn/GuestCheckedOut são publicados pelo Bounded Context Guest Operations, não por Reservations — ver §30/§31 (GuestCheckedIn, real desde a Fase 10, Checkpoint 2, deixou de ser prospectivo).

Anexado como seção nova ao final do documento — deliberadamente não inserido como subseção do §14 (que é específico do módulo Property Management) e sem renumerar nenhuma seção existente, preservando as referências já registradas em outros documentos (ex.: ADR-013 cita §13.2/§19).

27.1 Payloads dos Eventos do Incremento 1 (Fase 3)

Nenhum dos três eventos inclui nome do hóspede, telefone do hóspede, quantidade de hóspedes, ou qualquer valor anterior/posterior de campo alterado — apenas identificadores e, para ReservationUpdated, códigos estáveis de campo alterado — mesma regra do §13.1/§13.3/§14.1/§14.3. **Atualizado pela Fase 7, Incremento 2 (Dashboard & Reporting Foundation), Checkpoint 0/1 — ver §27.3**: ReservationCreated/ReservationUpdated passaram a incluir CheckInAt/CheckOutAt (a dimensão temporal operacional, nunca PII) — datas de check-in/check-out não fazem mais parte da lista de conteúdo permanentemente excluído.

Convenção comum aos três:

- AggregateType/AggregateId representam a Reserva afetada (AggregateId = ReservationId).
- ActorType é sempre "User". ActorId é o id do Administrador/Operador que executou a ação (RESERVATIONS:MANAGE).
- ReservationUpdated nunca é publicado quando a operação não altera estado efetivamente (PATCH idempotente — nenhum campo suprido difere, após normalização, do valor atual), nem quando a atualização é rejeitada por conflito de concorrência (snapshot desatualizado) — ver Fase 3 §3.
- ReservationCancelled é publicado exatamente uma vez por cancelamento efetivamente confirmado — nunca em uma rejeição (reserva inexistente, já cancelada).

ReservationCreated
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | Redundante com AggregateId |
| PropertyId | Guid | Identificador opaco do Imóvel — nunca código/nome/endereço |
| Status | string | Código estável do status inicial — sempre "confirmed" neste incremento (toda Reserva nasce Confirmed) |
| CheckInAt | DateTimeOffset? | (Fase 7, Incremento 2, §27.3) Valor real de `Reservation.CheckInAt`. Nullable apenas por compatibilidade aditiva — o produtor sempre popula com o valor real; nulo significa "evento anterior a esta mudança", nunca "desconhecido por design" |
| CheckOutAt | DateTimeOffset? | (Fase 7, Incremento 2, §27.3) Valor real de `Reservation.CheckOutAt` — mesma regra de nullability de CheckInAt |

ReservationUpdated
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | Redundante com AggregateId |
| ChangedFields | string[] | Códigos estáveis dos campos efetivamente alterados, na ordem: "property_id", "guest_name", "guest_phone", "check_in_at", "check_out_at", "guest_count" — nunca o valor novo nem o anterior |
| CheckInAt | DateTimeOffset? | (Fase 7, Incremento 2, §27.3) Valor ATUAL de `Reservation.CheckInAt` após esta atualização — sempre populado, independentemente de qual campo específico mudou (nunca exige diff no consumidor). Mesma regra de nullability de ReservationCreated |
| CheckOutAt | DateTimeOffset? | (Fase 7, Incremento 2, §27.3) Valor ATUAL de `Reservation.CheckOutAt` após esta atualização — mesma regra de CheckInAt acima |

ReservationCancelled
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | Redundante com AggregateId |
| PropertyId | Guid | Identificador opaco do Imóvel no momento do cancelamento |

27.3 Amendment — Fase 7, Incremento 2 (Dashboard & Reporting Foundation), Checkpoint 0/1

Decisão do usuário (Checkpoint 0, decisão 2): alteração aditiva aprovada em ReservationCreated/ReservationUpdated para transportar CheckInAt/CheckOutAt — a dimensão temporal operacional que o novo Bounded Context Dashboard & Reporting precisa para materializar indicadores de check-ins/check-outs/reservas futuras/canceladas sem se tornar um HTTP aggregator sobre Reservations (Architecture Principles §14 — Dashboard & Reporting é um contexto terminal, apenas consome eventos). GuestName/GuestPhone/GuestCount permanecem permanentemente excluídos — nenhuma decisão os aprovou.

Compatibility gate executado antes da alteração: verificado que o único consumidor real de ReservationCreated/ReservationCancelled (Housekeeping, `ReservationProjectionAndCancellationReaction`) lê exclusivamente TenantId/ReservationId/EventId/CorrelationId — nunca PropertyId/Status, e certamente nunca os novos campos — tornando a alteração aditiva segura sem exigir nenhuma migração de consumer existente. `ReservationUpdated` não tinha, até este checkpoint, nenhum consumidor real no código (`IIntegrationEventHandler<ReservationUpdated>` não existia em lugar nenhum), tornando essa alteração ainda mais segura. Serializador (System.Text.Json, via Wolverine) ignora campos desconhecidos por padrão — compatível em ambas as direções (consumidor novo lendo envelope antigo recebe `null`; consumidor antigo lendo envelope novo ignora os campos extras). Nenhum `ReservationCreatedV2` foi criado — desnecessário dado que a alteração é puramente aditiva e compatível.

27.2 Roteamento RabbitMQ dos Eventos do Incremento 1 (Fase 3)

Convenção já registrada em ADR-013 (exchange único por Bounded Context, tipo topic, routing key = nome do evento em snake_case) — mesmo padrão dos contextos Identity & Access (§13.2/§13.4) e Property Management (§14.2), aplicado aqui ao próprio exchange reservation-events, nunca reaproveitando identity-events/property-management-events.

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| ReservationCreated | reservation-events | reservation_created | Incremento 1 (Fase 3) |
| ReservationUpdated | reservation-events | reservation_updated | Incremento 1 (Fase 3) |
| ReservationCancelled | reservation-events | reservation_cancelled | Incremento 1 (Fase 3) |
| ReservationClosed | reservation-events | reservation_closed | Fase 10, Checkpoint 1 |

27.4 ReservationClosed — novo evento (Fase 10, Checkpoint 1 — Guest Operations Foundation)

Publicado quando uma Reservation transiciona para `Closed` (o checkout real do hóspede) — terminal, nunca em uma tentativa rejeitada/já-fechada (ver ADR-024). Mirra o payload de ReservationCancelled exatamente. ActorType é sempre "System" — esta transição não tem ator humano, é disparada exclusivamente pela cadeia interna Guest Operations → Workflow → Reservations (`CloseReservation`, um comando cross-context, nunca um Integration Event — não catalogado aqui pela mesma razão que `CreateCleaningForReservation` da ADR-018 não é; ver ADR-024).

ReservationClosed
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | Redundante com AggregateId |
| PropertyId | Guid | Identificador opaco do Imóvel no momento do fechamento |

28. Evento do Módulo Configuration & Policy — Payload e Roteamento (Fase 5, Incremento 1, Checkpoint 6)

Registrado neste documento antes da implementação, conforme Fase 5, Incremento 1, oficial decisão 5 e §10 da instrução de aprovação ("Registrar antes da implementação no Documento 07: PolicyUpdated"). Único evento deste incremento — não há um evento por nível de escopo (Global/Tenant/Property); o escopo pertence ao payload, nunca ao nome do evento. `ConfigurationUpdated` (para um incremento futuro de Configurações, ainda não implementado) foi aprovado na mesma decisão, mas não é registrado aqui por não ter payload/roteamento definidos até ser implementado — mesma convenção do §5/§27 para eventos prospectivos.

Anexado como seção nova ao final do documento, sem renumerar nenhuma seção existente — mesmo critério já usado para o §27 (Reservations).

28.1 Payload de PolicyUpdated

Payload mínimo, deliberadamente sem valor anterior, valor novo, condições, ações, dados pessoais ou motivo da alteração (Fase 5, Incremento 1, oficial decisão 5) — um consumidor que precisar do valor efetivo deve consultar a API administrativa/os leitores tipados, nunca inferir do próprio evento.

AggregateType = "PolicyValue"; AggregateId = id da linha `PolicyValue` recém-criada. ActorType é sempre "User" (toda escrita deste incremento é administrativa, via `POLICIES:MANAGE`); ActorId é o id do usuário administrador que criou a nova versão. TenantId (herdado de `IntegrationEvent`) é sempre preenchido neste incremento — GLOBAL nunca é editável por um tenant (decisão 2.2), então toda publicação de `PolicyUpdated` neste incremento se origina de uma escrita em escopo Tenant ou Property, ambos sempre tenant-scoped. `Timestamp` (herdado de `IntegrationEvent`) representa `occurredAtUtc`.

| Campo | Tipo | Descrição |
|---|---|---|
| PolicyCode | string | Código estável da política alterada — "EARLY_CHECKIN" ou "LATE_CHECKOUT" neste incremento |
| ScopeType | string | "Tenant" ou "Property" — nunca "Global" neste incremento |
| ScopeReferenceId | Guid? | Id do Imóvel quando ScopeType = "Property"; null quando ScopeType = "Tenant" |
| PolicyVersion | int | O número de versão da nova linha `PolicyValue` (nunca reaproveita `IntegrationEvent.Version`, que é a versão do envelope do evento, não do dado de negócio) |

28.2 Roteamento RabbitMQ de PolicyUpdated

Mesma convenção do ADR-013 — exchange próprio `configuration-events` (declarado desde o Checkpoint 1 deste incremento, nunca reaproveitando identity-events/property-management-events/reservation-events), tipo topic, routing key = nome do evento em snake_case.

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| PolicyUpdated | configuration-events | policy_updated | Checkpoint 6 (Fase 5, Incremento 1) |

28.3 Consumidor

Consumidor inicial e único neste incremento: invalidação de cache dentro do próprio contexto Configuration & Policy (processo `IHostPro.Worker`) — nunca um consumidor em Reservations ou qualquer outro Bounded Context (§10 da instrução de aprovação: "Não criar consumidor em Reservations neste incremento").

29. Eventos do Módulo Faxinas — Payload e Roteamento reais consumidos pela Agenda (Fase 7, Incremento 1 — Agenda Foundation)

Complementa a lista prospectiva do §6 ("Eventos do Módulo Faxinas") com o payload e roteamento reais dos eventos de Faxinas efetivamente consumidos pela Agenda — registrados agora porque a Fase 7 (Agenda Foundation) é o primeiro consumidor externo real destes eventos, fora do próprio contexto Housekeeping. Inicialmente esta seção cobria somente CleaningCreated (Checkpoint 1, gate de transporte real); estendida no fechamento do Checkpoint 1 (§29.4-§29.7) para cobrir a cobertura completa de transições de `Cleaning.Status` que a projeção da Agenda precisa refletir. `CleaningDelayed` permanece documentado apenas pelo nome no §6 — não consumido pela Agenda (ver §29.8). `CleaningStarted`/`CleaningInspectionStarted`/`CleaningCompleted`/`CleaningCancelled` também permanecem documentados apenas pelo nome no §6 — cada payload é idêntico em forma ao de `CleaningCreated`/`CleaningAssigned` (somente `CleaningId`, herdado do envelope), sem necessidade de retro-documentação individual.

Anexado como seção nova ao final do documento, sem renumerar nenhuma seção existente — mesmo critério já usado para o §27 (Reservations) e §28 (Configuration & Policy).

29.1 Payload de CleaningCreated

AggregateType = "Cleaning"; AggregateId = id da Cleaning recém-criada. ActorType é sempre "User" (criação é sempre administrativa/manual neste incremento — Fase 6, Checkpoint 0 gate); ActorId é o id do Administrador/Operador que criou a Cleaning.

| Campo | Tipo | Descrição |
|---|---|---|
| CleaningId | Guid | Redundante com AggregateId |
| PropertyId | Guid | Identificador opaco do Imóvel — nunca código/nome/endereço |
| ReservationId | Guid? | Id da Reserva referenciada; null quando a Cleaning foi criada sem referência a uma Reserva — nunca inferido |
| Status | string | Código estável do status inicial — sempre "Pending" (toda Cleaning nasce Pending) |
| ScheduledAtUtc | DateTimeOffset? | Horário operacional agendado da Cleaning — originado de `Cleaning.ScheduledAtUtc`, nunca recalculado a partir de `Reservation.CheckOutAt`, `DateTime.UtcNow`, do momento de criação ou do `Timestamp` de publicação do próprio evento (que é apenas o instante de publicação, não a data agendada). Deliberadamente opcional: uma Cleaning pode ser criada sem horário agendado (`CreateCleaningRequest` já permite isso desde o Incremento 2A — "quando omitido, a faxina simplesmente não participa da ordenação por agenda até um administrador defini-lo"); um consumidor deve tratar `null` como "sem horário conhecido", nunca como uma data padrão/`DateTimeOffset.MinValue`. Adicionado neste incremento — envelopes publicados antes desta mudança não possuem este campo e desserializam-no como `null`, tratado exatamente da mesma forma. |

29.2 Roteamento RabbitMQ de CleaningCreated

Mesma convenção do ADR-013 — exchange `housekeeping-events` (já declarado desde a Fase 6, Incremento 1), tipo topic, routing key = nome do evento em snake_case. Roteamento em si não foi alterado por este incremento — apenas o payload.

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| CleaningCreated | housekeeping-events | cleaning_created | Fase 6, Incremento 1 (payload original); campo `ScheduledAtUtc` adicionado na Fase 7, Incremento 1 |

29.3 Consumidor

Novo consumidor a partir deste incremento: projeção local assíncrona dentro do contexto Reservation & Scheduling (implementação técnica `Reservations`, processo `IHostPro.Worker`), usada exclusivamente para compor a Agenda unificada (Fase 7, Incremento 1). Nunca uma leitura síncrona — Reservations/Agenda não acessa `HousekeepingDbContext` nem qualquer API do Housekeeping diretamente (proibição absoluta, `Architecture Principles.md §14`).

29.4 CleaningNeedsHelp e CleaningNeedsMaterial — correção de roteamento (Checkpoint 1, fechamento)

Ambos os eventos já existiam desde o Incremento 2A (Portal da Faxineira) — publicados por `MarkCleaningWaitingHelpCommandHandler`/`MarkOwnCleaningWaitingHelpCommandHandler` (`CleaningNeedsHelp`) e seus equivalentes de Material (`CleaningNeedsMaterial`) — mas `IHostPro.Api/Program.cs` nunca os roteava para a exchange `housekeeping-events`: eram enfileirados no outbox do Housekeeping, porém nunca efetivamente entregues ao broker. Defeito real encontrado durante o fechamento do Checkpoint 1 da Agenda Foundation, corrigido registrando a regra de roteamento oficial idêntica à dos demais eventos de Faxinas.

Payload: apenas `CleaningId` (herdado do envelope) — nenhum campo adicional, mesma forma de `CleaningStarted`/`CleaningCancelled`.

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| CleaningNeedsHelp | housekeeping-events | cleaning_needs_help | Incremento 2A (payload); roteamento corrigido na Fase 7, Incremento 1, Checkpoint 1 |
| CleaningNeedsMaterial | housekeeping-events | cleaning_needs_material | Incremento 2A (payload); roteamento corrigido na Fase 7, Incremento 1, Checkpoint 1 |

Consumidor: a mesma projeção local da Agenda (§29.3) — aplica `Status = WaitingHelp`/`Status = WaitingMaterials` (nomes reais do enum `CleaningStatus`, `Documento 06 §5`) à linha já existente (criada por `CleaningCreated`).

29.5 CleaningInTransit — novo evento (Checkpoint 1, fechamento)

Publicado quando uma Cleaning transiciona `Assigned` → `InTransit` (self-service, `MarkOwnCleaningInTransitCommandHandler`) — transição real do aggregate que, até este fechamento, não publicava evento algum. Aprovado porque a Agenda é agora um consumidor externo legítimo do lifecycle e, sem o evento, não existe forma arquiteturalmente válida (sem consulta síncrona) de manter a projeção correta.

Payload: apenas `CleaningId` — deliberadamente sem `PropertyId`/`HousekeeperUserId`/descrição: o único consumidor real só precisa identificar a Cleaning e aplicar a transição a uma linha que já possui.

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| CleaningInTransit | housekeeping-events | cleaning_in_transit | Fase 7, Incremento 1, Checkpoint 1 (novo) |

Consumidor: a mesma projeção local da Agenda — aplica `Status = InTransit`.

29.6 CleaningInterrupted — novo evento (Checkpoint 1, fechamento)

Publicado quando uma Cleaning transiciona `Started` → `Interrupted` (administrativo, `MarkCleaningInterruptedCommandHandler`) — mesma decisão e mesma justificativa do §29.5. Até este fechamento, esta transição não publicava evento algum (registrado assim desde a Fase 6).

Payload: apenas `CleaningId` — deliberadamente sem causa/motivo: o único consumidor real só precisa identificar a Cleaning e aplicar a transição.

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| CleaningInterrupted | housekeeping-events | cleaning_interrupted | Fase 7, Incremento 1, Checkpoint 1 (novo) |

Consumidor: a mesma projeção local da Agenda — aplica `Status = Interrupted`.

29.7 Matriz completa de cobertura de Status da Agenda

| Transição real do aggregate | Integration Event | Routing Key | Consumido pela Agenda | Status resultante na projeção |
|---|---|---|---|---|
| Nascimento | CleaningCreated | cleaning_created | Sim | Pending |
| Pending → Assigned | CleaningAssigned | cleaning_assigned | Sim | Assigned |
| Assigned → InTransit | CleaningInTransit | cleaning_in_transit | Sim | InTransit |
| Assigned/InTransit → Started | CleaningStarted | cleaning_started | Sim | Started |
| Started → InInspection | CleaningInspectionStarted | cleaning_inspection_started | Sim | InInspection |
| InInspection → Completed | CleaningCompleted | cleaning_completed | Sim | Completed |
| Started → Interrupted | CleaningInterrupted | cleaning_interrupted | Sim | Interrupted |
| Started → WaitingMaterials | CleaningNeedsMaterial | cleaning_needs_material | Sim | WaitingMaterials |
| Started → WaitingHelp | CleaningNeedsHelp | cleaning_needs_help | Sim | WaitingHelp |
| Pending/Assigned → Cancelled | CleaningCancelled | cleaning_cancelled | Sim | Cancelled |

Todas as dez transições reais de `Cleaning.Status` (`Documento 06 §5`, enum `CleaningStatus`) publicam um Integration Event roteado para `housekeeping-events` e são refletidas pela projeção da Agenda — nenhum status "best effort"/aproximado permanece.

29.8 CleaningDelayed — não consumido (justificativa explícita)

`CleaningDelayed` não é consumido pela Agenda porque não altera `Cleaning.Status` nem qualquer outro campo materializado pela projeção — `ReportOwnCleaningDelayCommandHandler` nunca chama um método de transição de `Cleaning`, apenas publica uma notificação. Consumi-lo seria um no-op sem efeito observável. Não foi inventada uma nova data por inferência a partir deste evento.

29.9 CleaningOccurrenceRegistered — novo evento (Fase 7, Incremento 2 — Dashboard & Reporting Foundation, Checkpoint 0/1 decisão 3)

Publicado quando um(a) faxineiro(a) registra uma `CleaningOccurrence` contra sua própria Cleaning (`RegisterCleaningOccurrenceCommandHandler`, Fase 6, Incremento 2A). Produtor: Housekeeping. Consumidor inicial: Dashboard & Reporting (novo Bounded Context, Supporting/terminal — Architecture Principles §3/§14).

Payload:
| Campo | Tipo | Descrição |
|---|---|---|
| OccurrenceId | Guid | Redundante com AggregateId |
| CleaningId | Guid | A Cleaning à qual a ocorrência está vinculada |
| OccurrenceType | string | Código estável do tipo (Theft, Breakage, ForgottenObject, Damage, Animal, Smoking, Noise, MaterialShortage — `OccurrenceTypeCodeMapper`) |
| RegisteredAtUtc | DateTimeOffset | O timestamp real de `CleaningOccurrence.RegisteredAtUtc` — produzido diretamente na transição, nunca inferido |

Deliberadamente excluído: `Description` (texto livre, pode conter PII/conteúdo sensível), `RegisteredByUserName` (Dashboard não armazena dados de exibição de usuário — Checkpoint 0 decisão 17), `PropertyId` (Dashboard resolve via `CleaningId` contra sua própria projeção local de Cleaning, nunca duplicado aqui), e qualquer referência de evidência/foto (Files permanece fora de escopo).

Roteamento RabbitMQ: exchange `housekeeping-events` (o mesmo exchange dos dez eventos de ciclo de vida de `Cleaning` — §29.1-§29.7), routing key `cleaning_occurrence_registered`.

Não foi criado nenhum outro evento de Occurrence (ex.: `OccurrenceResolved`) — `CleaningOccurrence` não possui lifecycle de resolução documentado neste incremento (Checkpoint 0 decisão 3).

30. Evento do Módulo Guest Operations — Payload e Roteamento (Fase 10, Checkpoint 1 — Guest Operations Foundation)

Formalizado em GuestOperations.Contracts (ADR-024). Publicado quando um `GuestStayOperation` transiciona para `CheckedOut`. Produtor: Guest Operations (`RecordGuestCheckedOutCommandHandler`). Consumidor: Workflow Orchestration (`GuestCheckedOutCloseReservationOrchestrator`), que envia o comando cross-context `CloseReservation` a Reservations em reação (ver §27.4) — nunca um consumo direto por Reservations.

30.1 Payload de GuestCheckedOut

ActorType é sempre "System" — nenhum ator HTTP existe ainda neste checkpoint (zero endpoints públicos). Sem PropertyId — nenhum consumidor real o exige (Reservations já o possui; `CloseReservation` não precisa dele).

GuestCheckedOut
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | A Reservation cujo checkout disparou este evento — não redundante com AggregateId (AggregateId é o id do próprio GuestStayOperation) |

30.2 Roteamento RabbitMQ de GuestCheckedOut

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| GuestCheckedOut | guest-operations-events | guest_checked_out | Fase 10, Checkpoint 1 |

30.3 Consumidor

Workflow Orchestration, fila `workflow.guest-checked-out-trigger` — segunda fila independente deste Bounded Context (a primeira é `workflow.reservation-created-trigger`, ADR-018). Guest Operations nunca precisa saber que Workflow está ouvindo — mesmo padrão de pub/sub desacoplado já usado por toda fila deste catálogo.

31. Evento GuestCheckedIn — Payload e Roteamento (Fase 10, Checkpoint 2 — Check-in/Checkout Core)

Formalizado em GuestOperations.Contracts (ADR-024, amendment Fase 10 Checkpoint 2). Publicado quando um `GuestStayOperation` transiciona para `CheckedIn`. Produtor: Guest Operations (`RecordGuestCheckedInCommandHandler`). Mirroring §30 (GuestCheckedOut) exatamente. **Atualizado na Fase 10, Checkpoint 4** (Portaria Notification Foundation): ganhou `PropertyId` (ver §37.1) e seu primeiro consumidor real (Communication, ver §37.2) — as afirmações "sem PropertyId"/"nenhum consumidor real" desta seção, válidas através do Checkpoint 3, não se aplicam mais.

31.1 Payload de GuestCheckedIn

ActorType é sempre "System" — nenhum ator HTTP é registrado no payload ainda (o endpoint HTTP que dispara este evento existe desde este Checkpoint, mas a identidade do chamador não é transportada no evento — mesma omissão de GuestCheckedOut).

GuestCheckedIn
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | A Reservation cujo check-in disparou este evento — não redundante com AggregateId (AggregateId é o id do próprio GuestStayOperation) |
| PropertyId | Guid | Adicionado na Fase 10, Checkpoint 4 — permite que Communication resolva o contato de Portaria (ADR-026, Exceção Síncrona #9) sem uma segunda consulta. Provider-neutral, não-PII |
| CheckedInAtUtc | DateTimeOffset | O instante real do check-in, mesmo valor persistido em `GuestStayOperation.CheckedInAtUtc` |

31.2 Roteamento RabbitMQ de GuestCheckedIn

| Evento | Exchange | Routing Key | Registrado desde |
|---|---|---|---|
| GuestCheckedIn | guest-operations-events | guest_checked_in | Fase 10, Checkpoint 2 |

Mesma exchange de GuestCheckedOut (§30.2) — uma segunda routing key, nunca uma segunda exchange.

31.3 Consumidor

**Atualizado na Fase 10, Checkpoint 4**: Communication (`GuestCheckedInFrontDeskNotificationProcessor`, fila `communication.guest-checked-in-trigger`, gated a `Development` — mesmo padrão de `communication.reservation-created-trigger`) é o primeiro consumidor real deste evento — ver §37.2. Através do Checkpoint 3, nenhuma fila estava vinculada à routing key `guest_checked_in` e uma mensagem publicada era simplesmente descartada pelo RabbitMQ, nunca um erro (mesmo comportamento documentado para `airbnb_sync_started`, §29) — esse comportamento permanece o histórico correto até o Checkpoint 4, não o estado atual.

32. Novo consumidor de ReservationCreated — Guest Operations (Fase 10, Checkpoint 2 — Check-in/Checkout Core)

`ReservationCreated` (§27.2) ganha um quinto consumidor independente em Fase 10, Checkpoint 2: Guest Operations (`ReservationCreatedGuestStayInitializer`), reagindo por coreografia (nunca via Workflow Orchestration) para criar automaticamente o `GuestStayOperation` correspondente — a decisão resolvida do gatilho de criação (ver ADR-024, amendment Fase 10 Checkpoint 2). Fila `guestoperations.reservation-created-trigger`, vinculada à exchange já existente `reservation-events` (mesma routing key `reservation_created`, nenhuma exchange nova). Idempotente por busca-antes-de-criar (nunca duplica um `GuestStayOperation` já existente para a mesma Reservation — a restrição única `(tenant_id, reservation_id)` já documentada em §30 permanece como defesa em profundidade, nunca a única garantia).

33. Eventos EarlyCheckinApproved / EarlyCheckinDenied — Payload e Roteamento (Fase 10, Checkpoint 3 — Early Check-in / Late Checkout)

Formalizados em GuestOperations.Contracts (ADR-024, amendment Fase 10 Checkpoint 3). Publicados por `RequestEarlyCheckInCommandHandler` ao decidir sincronamente um `EarlyCheckInRequest`. `EarlyCheckinDenied` fecha a assimetria do catálogo sinalizada desde o §31 (só existia o par Approved/Denied para nenhum dos dois fluxos de Guest Operations até este Checkpoint).

33.1 Payload de EarlyCheckinApproved

ActorType é sempre "System" — a decisão é automática, síncrona, na mesma requisição de sua própria criação, nunca um passo de aprovação humana distinto.

EarlyCheckinApproved
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | A Reservation cujo check-in antecipado foi aprovado — não redundante com AggregateId (AggregateId é o id do próprio EarlyCheckInRequest) |
| PropertyId | Guid | Adicionado na Fase 10, Checkpoint 4 — permite que Communication resolva o contato de Portaria (ADR-026, Exceção Síncrona #9) sem uma segunda consulta |
| ApprovedCheckInAt | DateTimeOffset | O novo horário de check-in aprovado — o mesmo valor que Workflow Orchestration usa para reagendar a Reservation real |

33.2 Payload de EarlyCheckinDenied

EarlyCheckinDenied
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | A Reservation cujo pedido de check-in antecipado foi negado |
| ReasonCode | string | Um dos códigos estáveis de `EarlyCheckinDeniedReasonCodes` (`policy_not_configured`, `policy_not_allowed`, `before_earliest_time`, `schedule_conflict`, `cleaning_not_ready`) — nunca texto livre |

33.3 Roteamento RabbitMQ

| Evento | Exchange | Routing Key | Registrado desde | Consumidor |
|---|---|---|---|---|
| EarlyCheckinApproved | guest-operations-events | early_checkin_approved | Fase 10, Checkpoint 3 | Dois desde a Fase 10, Checkpoint 4 (cada um em fila própria, ADR-020): Workflow Orchestration (`workflow.early-checkin-approved-trigger`, envia `RescheduleReservationForEarlyCheckIn` a Reservations); Communication (`communication.early-checkin-approved-trigger`, `EarlyCheckinApprovedFrontDeskNotificationProcessor`, ver §37.2) |
| EarlyCheckinDenied | guest-operations-events | early_checkin_denied | Fase 10, Checkpoint 3 | Nenhum — mesma convenção de `guest_checked_in` (§31.3): rota declarada, sem fila vinculada; mensagem publicada é descartada, nunca um erro |

Mesma exchange de GuestCheckedOut/GuestCheckedIn (§30.2/§31.2) — duas novas routing keys, nunca uma segunda exchange.

34. Eventos LateCheckoutApproved / LateCheckoutDenied — Payload e Roteamento (Fase 10, Checkpoint 3 — Early Check-in / Late Checkout)

Formalizados em GuestOperations.Contracts (ADR-024, amendment Fase 10 Checkpoint 3). Publicados por `RequestLateCheckoutCommandHandler` ao decidir sincronamente um `LateCheckoutRequest`. `LateCheckoutApproved` nunca é publicado para o desfecho `PendingPayment` — apenas para uma aprovação final e definitiva (ver §5.6/§5.7 da homologação da Fase 10 para a fronteira exata de PIX).

34.1 Payload de LateCheckoutApproved

LateCheckoutApproved
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | A Reservation cujo checkout tardio foi aprovado |
| PropertyId | Guid | Adicionado na Fase 10, Checkpoint 4 — permite que Communication resolva o contato de Portaria (ADR-026, Exceção Síncrona #9) sem uma segunda consulta |
| ApprovedCheckOutAt | DateTimeOffset | O novo horário de checkout aprovado — o mesmo valor que Workflow Orchestration usa para reagendar a Reservation real |
| UpdatesCleaning | bool | Snapshot da política `LateCheckoutPolicy.UpdatesCleaning` no momento da decisão — o único gate da reação de Housekeeping (§34.3); nunca persistido no agregado `LateCheckoutRequest`, apenas transportado neste evento. Nunca lido pela reação de Communication (§37.2) — os dois consumidores têm gates independentes |

34.2 Payload de LateCheckoutDenied

LateCheckoutDenied
| Campo | Tipo | Descrição |
|---|---|---|
| ReservationId | Guid | A Reservation cujo pedido de checkout tardio foi negado |
| ReasonCode | string | Um dos códigos estáveis de `LateCheckoutDeniedReasonCodes` (`policy_not_configured`, `policy_not_allowed`, `after_latest_time`, `schedule_conflict`) — nunca texto livre |

34.3 Roteamento RabbitMQ

| Evento | Exchange | Routing Key | Registrado desde | Consumidores |
|---|---|---|---|---|
| LateCheckoutApproved | guest-operations-events | late_checkout_approved | Fase 10, Checkpoint 3 | Três desde a Fase 10, Checkpoint 4, cada um em fila própria (ADR-020): Workflow Orchestration (`workflow.late-checkout-approved-trigger`, sempre — envia `RescheduleReservationForLateCheckout` a Reservations); Housekeeping (`housekeeping.late-checkout-approved-trigger`, gated em `UpdatesCleaning=true` — registra apenas uma `CleaningAuditEntry`, nunca muta `Cleaning.ScheduledAtUtc`, ver §29 e ADR-024 amendment §B7); Communication (`communication.late-checkout-approved-trigger`, `LateCheckoutApprovedFrontDeskNotificationProcessor`, nunca gated em `UpdatesCleaning` — ver §37.2) |
| LateCheckoutDenied | guest-operations-events | late_checkout_denied | Fase 10, Checkpoint 3 | Nenhum — mesma convenção de `EarlyCheckinDenied` (§33.3) |

Isolamento dos dois consumidores de `LateCheckoutApproved` provado empiricamente contra broker real (nunca apenas por inspeção estática) — ver homologação da Fase 10, §5.8.

35. Comandos cross-context RescheduleReservationForEarlyCheckIn / RescheduleReservationForLateCheckout (Fase 10, Checkpoint 3)

Formalizados em Reservations.Contracts (ADR-024, amendment Fase 10 Checkpoint 3; ADR-018). Nunca Integration Events — comandos cross-context enviados exclusivamente por Workflow Orchestration via `IMessageBus.SendAsync`, mesma exchange `workflow-orchestration-commands` já criada pelo ADR-018, entregues à mesma fila `reservations.workflow-commands` que já recebe `CloseReservation` (§27.4).

| Comando | Exchange | Routing Key | Fila |
|---|---|---|---|
| RescheduleReservationForEarlyCheckIn | workflow-orchestration-commands | reschedule_for_early_check_in | reservations.workflow-commands |
| RescheduleReservationForLateCheckout | workflow-orchestration-commands | reschedule_for_late_checkout | reservations.workflow-commands |

Cada um publica `ReservationUpdated` (§27.3) ao mutar a Reservation real — nunca um evento novo (decisão do usuário: a Reservation continua a única fonte de verdade da agenda) — com `ChangedFields=["check_in_at"]`/`["check_out_at"]` respectivamente, `ActorType="System"`.

36. Consumidor síncrono ICleaningReadinessReader — primeira exceção síncrona de Housekeeping (Fase 10, Checkpoint 3)

Não é um Integration Event nem um comando — registrado aqui por completude do catálogo de comunicação cross-context. `Housekeeping.Contracts.ICleaningReadinessReader` (ADR-024 amendment; Architecture Principles §14, Exceção 8) é a primeira interface de leitura síncrona que Housekeeping concede a outro Bounded Context — consumida exclusivamente por Guest Operations, para decidir `EarlyCheckinDenied` com `ReasonCode=cleaning_not_ready` (§33.2). Ver `IReservationScheduleReader` (Exceção 7, mesmo amendment) para o par de leituras síncronas deste Checkpoint.

37. Portaria Notification Foundation — PropertyId nos eventos de Guest Operations e novos consumidores de Communication (Fase 10, Checkpoint 4)

37.1 Extensão de payload — PropertyId

`GuestCheckedIn` (§31), `EarlyCheckinApproved` (§33) e `LateCheckoutApproved` (§34) ganharam o campo `required Guid PropertyId` — extensão aditiva, mesmo precedente de `ReservationCreated` ganhar `CheckInAt`/`CheckOutAt` na Fase 7, Incremento 2 (§27.3). Motivo: Communication precisa de `PropertyId` para resolver o contato de Portaria via `IFrontDeskContactReader` (ADR-026, Exceção Síncrona #9, ver §38) sem uma segunda consulta síncrona. `PropertyId` já estava disponível nos três handlers produtores (via `GuestStayOperation.PropertyId`) — nenhuma nova leitura síncrona foi criada apenas para preencher este campo. `GuestName`/`GuestPhone`/`CondominiumId`/`AccessCredential` NUNCA foram adicionados a nenhum dos três eventos.

37.2 Novos consumidores de Communication

Três novos processadores, um por business intent (`GuestCheckedInFrontDeskNotificationProcessor`, `EarlyCheckinApprovedFrontDeskNotificationProcessor`, `LateCheckoutApprovedFrontDeskNotificationProcessor`), cada um resolvendo o contato de Portaria via `IFrontDeskContactReader` (ADR-026) a partir do `PropertyId` do evento, o nome do hóspede via `IReservationGuestContactReader` (ADR-019, estendido — ver §39), renderizando um Template (`FRONT_DESK_GUEST_CHECKED_IN`/`FRONT_DESK_EARLY_CHECKIN_APPROVED`/`FRONT_DESK_LATE_CHECKOUT_APPROVED`) e criando/enviando uma `Message` através do mesmo `IOutboundMessageConnector`/`FakeWhatsAppConnector` já usado pelo fluxo de confirmação de reserva (Fase 9, Checkpoint 1). Gated a `Development` (mesmo padrão de `communication.reservation-created-trigger`) — nenhum provider real é chamado.

Quando nenhum `FrontDeskContact` ativo é resolvido (Condomínio não configurado, Property sem Condomínio, ou contato `IsActive=false`), o processador correspondente é um no-op deliberado: zero `Message` criada, log estruturado com `FrontDeskContactNotConfigured`, zero retry — mesma disciplina de "no-op para uma condição operacional esperada" já usada para `NoActiveTemplate` no fluxo de confirmação de reserva.

38. Consumidor síncrono IFrontDeskContactReader — Exceção Síncrona #9 (Fase 10, Checkpoint 4, ADR-026)

Não é um Integration Event nem um comando — registrado aqui por completude do catálogo de comunicação cross-context, mesma convenção de §36. `PropertyManagement.Contracts.IFrontDeskContactReader.GetActiveByPropertyIdAsync(TenantId, PropertyId)` (ADR-026; Architecture Principles §14, Exceção 9) é consumido exclusivamente por Communication, retornando `FrontDeskContactReadResult(ContactId, DisplayName?, PhoneNumber)`. Resolução interna Property → Condomínio → contato ativo é responsabilidade exclusiva de Property Management. Property sem Condomínio, Condomínio sem contato configurado e contato inativo colapsam no mesmo resultado `null`.

39. Extensão factual de ADR-019 — GuestName em ReservationGuestContact (Fase 10, Checkpoint 4)

`Reservations.Contracts.ReservationGuestContact` (ADR-019, Exceção Síncrona #5/#4 conforme numeração) ganhou um terceiro campo, `GuestName` (string, não-nulo), para que os três processadores de Portaria (§37.2) possam renderizar o nome do hóspede. O boundary permanece exatamente o mesmo (Communication → Reservations, um único consumidor) — não é uma nova exceção síncrona. `GuestPhone` nunca é usado pelos processadores de Portaria. Ver amendment ao final de ADR-019 para o registro completo desta extensão.

40. LateCheckoutPaymentRequired (Fase 10, Checkpoint 5 — PIX/Payment Deterministic Foundation)

Publicado por `RequestLateCheckoutCommandHandler` (GuestOperations.Contracts) quando `LateCheckoutRequest` entra em `PendingPayment` — supersede o comportamento do Checkpoint 3, que não publicava nada nesse ramo (decisão async aprovada no CP5). Payload: `TenantId`, `LateCheckoutRequestId`, `ReservationId`, `Amount`, `CurrencyCode` (sempre `"BRL"` neste checkpoint), `OccurredAtUtc`, metadados padrão. Deliberadamente provider-neutro e sem PII/QR/dado de provider. Exchange `guest-operations-events`, routing key `late_checkout_payment_required`, consumida exclusivamente por Payments (`payments.late-checkout-payment-required-trigger`).

| Evento | Exchange | Routing Key | Fila |
|---|---|---|---|
| LateCheckoutPaymentRequired | guest-operations-events | late_checkout_payment_required | payments.late-checkout-payment-required-trigger |

41. PixChargeCreated / PixChargeConfirmed (Fase 10, Checkpoint 5, Payments.Contracts)

Estes dois eventos SUPERSEDEM os nomes prospectivos listados em §9 (`PixPaymentReceived`, `PixPaymentConfirmed`) — que existiam apenas como lista de nomes, sem payload, sem contexto de publicação, desde antes da Fase 10. A decisão do CP5: o conceito real é `PixChargeConfirmed`; `PixPaymentReceived`/`PixPaymentConfirmed` (§9) nunca serão materializados como eventos distintos. `PaymentFailed`/`RefundRequested`/`RefundApproved`/`RefundRejected` (§9) permanecem prospectivos, sem mudança — Refunds continua DEFERRED (não reaberto neste checkpoint); `PaymentFailed` como evento publicado não foi criado (mandato item 33: "não criar mensagens adicionais só para aumentar cobertura" — nenhum consumidor real precisa dele; o status `Failed` é lido diretamente do estado persistido de `PixCharge` quando necessário).

`PixChargeCreated` — publicado quando o (fake, este checkpoint) provider aceita a criação da cobrança. Payload: `TenantId`, `AggregateId` (o `PixChargeId`), `AggregateType="PixCharge"`, `LateCheckoutRequestId`, `ReservationId`, metadados padrão. Deliberadamente livre de `QrCodePayload`/`ProviderChargeId`/qualquer payload financeiro (ADR-025/ADR-027) — Communication resolve isso separadamente, de forma síncrona, via `IPixChargeDeliveryReader` (§43). Consumida exclusivamente por Communication (`communication.pixcharge-created-trigger`, gated a `Development`, mesmo padrão de `communication.reservation-created-trigger`).

`PixChargeConfirmed` — publicado quando `PixCharge.Confirm()` aplica uma transição genuína (nunca em uma confirmação duplicada — idempotente). Payload: `TenantId`, `AggregateId` (o `PixChargeId`), `AggregateType="PixCharge"`, `LateCheckoutRequestId`, `ReservationId`, `ConfirmedAtUtc`, metadados padrão. Consumida exclusivamente por Guest Operations (`guestoperations.pixcharge-confirmed-trigger`, incondicional — sem distinção fake/real de provider que justifique gate).

| Evento | Exchange | Routing Key | Fila |
|---|---|---|---|
| PixChargeCreated | payments-events | pix_charge_created | communication.pixcharge-created-trigger |
| PixChargeConfirmed | payments-events | pix_charge_confirmed | guestoperations.pixcharge-confirmed-trigger |

42. PixChargeConfirmationReceived — seam de confirmação provider-neutro (Fase 10, Checkpoint 5)

Não é um Integration Event (não herda `IntegrationEvent` — mirroring `CreateCleaningForReservation`/`CloseReservation`, mensagens cross-context que vivem no `.Contracts` do contexto RECEPTOR). `Payments.Contracts.PixChargeConfirmationReceived` representa o fato provider-neutro "uma cobrança PIX foi confirmada" — o seam que uma futura normalização de webhook em External Integrations produziria, sem qualquer mudança no domínio Payments. Payload: `TenantId`, `PixChargeId`, `ConfirmedAtUtc`, `CorrelationId`, `CausationId?` — sem payload de provider, sem QR, sem PII de pagador. Exchange dedicada `payments-commands` (Direct, mesma razão estrutural de `workflow-orchestration-commands` — destino único, nunca fan-out), routing key `pix_charge_confirmation_received`, fila `payments.confirmation-received`. Único publicador neste checkpoint: o harness de teste E2E, via envio real Wolverine/RabbitMQ — nunca um endpoint HTTP test-only (ver ADR-025 §"Confirmação — o seam provider-neutro" para o registro completo da decisão).

| Mensagem | Exchange | Routing Key | Fila |
|---|---|---|---|
| PixChargeConfirmationReceived | payments-commands | pix_charge_confirmation_received | payments.confirmation-received |

43. Consumidores síncronos IPixProvider / IPixChargeDeliveryReader — Exceções Síncronas #10/#11 (Fase 10, Checkpoint 5)

Não são Integration Events nem comandos — registrados aqui por completude do catálogo de comunicação cross-context, mesma convenção de §36/§38. `ExternalIntegrations.Contracts.IPixProvider.CreateChargeAsync` (ADR-025; Architecture Principles §14, Exceção 10) é consumida exclusivamente por Payments, executando a criação de uma cobrança PIX através do (fake, este checkpoint) provider — nunca uma leitura. `Payments.Contracts.IPixChargeDeliveryReader.GetForDeliveryAsync(TenantId, PixChargeId)` (ADR-027; Architecture Principles §14, Exceção 11) é consumida exclusivamente por Communication, retornando `PixChargeDeliveryReadResult(PixChargeId, QrCodePayload, Amount, CurrencyCode, ExpiresAtUtc?)` — nunca o agregado `PixCharge`, nunca `ProviderChargeId`/`IdempotencyKey`. Cobrança inexistente para o tenant informado e cobrança sem `QrCodePayload` ainda persistido colapsam no mesmo resultado `null`.

44. `LateCheckoutRequest.Approve()` — extensão de estado de origem (Fase 10, Checkpoint 5)

Não é um evento — registrado aqui por completude. `GuestOperations.Domain.LateCheckoutRequest.Approve()` (Checkpoint 3) aceitava apenas `Pending` como estado de origem. O Checkpoint 5 estendeu o guard para também aceitar `PendingPayment` — a exata "transição em diante" que o próprio doc comment de `PendingPayment` já registrava desde o Checkpoint 3 como um estado não-terminal reservado a um checkpoint futuro. Nenhuma outra mudança de comportamento — `Approve()` continua terminal, `DecidedAtUtc`/`UpdatedAtUtc` continuam atualizados da mesma forma.

45. PixChargeFailureReceived / PixChargeExpirationReceived — extensão do seam provider-neutro (Fase 10, Checkpoint 5.1 — Payment Failure/Expiration Evidence Corrective Gate)

Não são Integration Events (mesma natureza de `PixChargeConfirmationReceived`, §42 — mensagens cross-context vivendo no `.Contracts` do contexto RECEPTOR, não herdam `IntegrationEvent`). Corrigem uma lacuna de evidência apontada na homologação do Checkpoint 5: o mandato exigia prova E2E real das transições `Failed`/`Expired`, e o relatório final do CP5 registrou `Failed/Expired E2E = NÃO PROVADO` (o `FakePixProvider` de produção sempre aceita, e nada no CP5 disparava essas transições fora de teste unitário). O CP5.1 fecha essa lacuna reaproveitando o MESMO seam provider-neutro de `PixChargeConfirmationReceived` — nunca uma arquitetura nova, nunca um provider real, nunca um webhook real.

`Payments.Contracts.PixChargeFailureReceived` — payload: `TenantId`, `PixChargeId`, `FailureCode?` (provider-neutro/sanitizado, usado apenas para diagnóstico/log — `PixCharge` não tem coluna para ele, mesmo tratamento que `PixChargeCreationResult.FailureCode` já recebia desde o CP5), `OccurredAtUtc`, `CorrelationId`, `CausationId?`. `Payments.Contracts.PixChargeExpirationReceived` — payload: `TenantId`, `PixChargeId`, `ExpiredAtUtc`, `CorrelationId`, `CausationId?`. Nenhum dos dois carrega QR, PII de pagador, ou dado de provider.

Os handlers (`PixChargeFailureReceivedCommandHandler`/`PixChargeExpirationReceivedCommandHandler`) chamam `PixCharge.Fail`/`PixCharge.Expire` — ambos idempotentes por construção — e **não publicam nenhum evento downstream** (nenhum consumidor real precisa reagir a `Failed`/`Expired`; `LateCheckoutRequest` permanece deliberadamente `PendingPayment`, sem `LateCheckoutDenied`). Mesma exchange Direct `payments-commands` de `PixChargeConfirmationReceived`, duas novas routing keys. Único publicador hoje: o harness de teste E2E (ver ADR-025, seção "Failure/Expiration — extensão do mesmo seam provider-neutro").

`PixCharge.Expire()` é um método de domínio novo neste checkpoint (mirroring `Fail()` exatamente: no-op se já `Confirmed`/`Failed`/`Expired`) — persiste `ExpiredAtUtc` (nova coluna nullable, migração `AddPixChargeExpiredAtUtc`, sem mudança de RLS/grants — ambos já se aplicam à tabela inteira).

| Mensagem | Exchange | Routing Key | Fila |
|---|---|---|---|
| PixChargeFailureReceived | payments-commands | pix_charge_failure_received | payments.failure-received |
| PixChargeExpirationReceived | payments-commands | pix_charge_expiration_received | payments.expiration-received |

46. GuestAccessDeliveryRequested / Exceção Síncrona #12 (Fase 10, Checkpoint 6.2 — Guest Access Secure Delivery Corrective Implementation)

Fecha o requisito interno de Fase 10 identificado no Checkpoint 6 (Access Credential/"Enviar senha", Documento 10 §11) e resolvido no Checkpoint 6.1 (Decision Gate): MVP = senha fixa por Property, configurada manualmente, sem Smart Lock, sem geração automática.

`GuestOperations.Contracts.GuestAccessDeliveryRequested` — Integration Event real, publicado por `RequestGuestAccessDeliveryCommandHandler` quando um administrador solicita explicitamente a entrega (nunca automático — nenhum publisher em `ReservationCreated`/`GuestCheckedIn`, nenhum scheduler). `AggregateId`/`AggregateType` são o id do `GuestStayOperation`/`"GuestStayOperation"`. Payload: `TenantId`, `ReservationId`, `PropertyId`, metadados padrão — deliberadamente sem credencial, sem referência de credencial, sem instruções, sem GuestName/GuestPhone. Exchange `guest-operations-events` (a mesma já existente), routing key `guest_access_delivery_requested`, consumida exclusivamente por Communication (`communication.guest-access-delivery-trigger`, gated a `Development`, mesmo padrão dos demais consumidores de Communication nesta exchange).

Precondições, validadas antes de publicar (nenhuma mutação de domínio própria): Reservation deve estar `confirmed` (via `IReservationScheduleReader`, Exceção #7 já aprovada — nenhuma exceção síncrona nova foi necessária aqui), `GuestStayOperation` não pode estar `CheckedOut` (`CheckedIn` é explicitamente permitido — reenviar acesso após o check-in é uma necessidade operacional legítima). Idempotência é responsabilidade exclusiva de Communication (chave por intent) — Guest Operations republica a cada chamada, sem rastrear "já solicitado".

`PropertyManagement.Contracts.IPropertyGuestAccessReader` (ADR-028; Architecture Principles §14, Exceção 12) — consumida exclusivamente por Communication, retornando `PropertyGuestAccessReadResult(AccessCredential?, AccessInstructions?)`. A credencial retornada já é o VALOR resolvido (nunca a referência) — resolvida internamente por `IPropertyAccessCredentialProvider` (abstração nova, independente de `IWhatsAppCredentialProvider`), nunca persistida de volta em nenhuma tabela. Configuração ausente/inativa → `null` (no-op ordinário); referência configurada mas não resolvível pelo provider → exceção (falha de infraestrutura, nunca engolida silenciosamente).

`Communication.Application.GuestAccessDeliveryProcessor` trata dois business intents independentes a partir do mesmo evento — `GUEST_ACCESS_CREDENTIAL` (sensível: o valor real é enviado ao `IOutboundMessageConnector`, mas o `Message` persistido recebe o marcador fixo `"[SENSITIVE CONTENT REDACTED]"` em vez do conteúdo real — nenhuma mudança ao agregado `Message`) e `GUEST_ACCESS_INSTRUCTIONS` (não-secreto, pipeline padrão, persistido normalmente). Chaves de idempotência distintas por `TemplateKey` — a ausência de um intent nunca bloqueia o outro.

| Evento/Contrato | Exchange | Routing Key | Fila |
|---|---|---|---|
| GuestAccessDeliveryRequested | guest-operations-events | guest_access_delivery_requested | communication.guest-access-delivery-trigger |

47. InboundGuestMessageReceived / Exceção Síncrona #13 (Fase 11, Checkpoint 1 — Inbound Conversation Foundation)

Fecha a fundação de recepção identificada no Checkpoint 0 (nenhuma mensagem inbound de convidado era processada até este ponto — ADR-022, item 19). Sem nenhuma lógica de IA (`AIImplemented=false`).

`ExternalIntegrations.Contracts.InboundGuestMessageReceived` — o segundo Integration Event que External Integrations publica (o primeiro foi `WhatsAppMessageStatusChanged`, §41 acima é sobre Payments; ver Checkpoint 2.3.3 da Fase 9 para o primeiro de External Integrations). Publicado por `WhatsAppWebhookMessageEventPublisher` após verificação de assinatura, resolução de tenant e normalização (`MetaWebhookMessageProcessor`, confinado a `Infrastructure.Meta`). Payload: `TenantId`, `ProviderMessageId`, `Channel`, `SenderPhoneNormalized` (dígitos-somente), `MessageType` (`Text`/`Unsupported`), `Text` (apenas para `Text`), `OccurredAtUtc` — deliberadamente sem payload bruto do Meta, sem credencial, sem PhoneNumberId da plataforma. CP1 é TEXT ONLY — qualquer outro tipo (imagem/áudio/vídeo/documento/localização) é `Unsupported`, nunca baixado/modelado (Documento 16 §31). Exchange `external-integrations-events` (a mesma já existente, ADR-022), routing key `inbound_guest_message_received`, consumida exclusivamente por Communication (`communication.inbound-guest-message-trigger`, unconditional — sem distinção fake/real de conector, mesmo padrão de `communication.whatsapp-status-projection`).

`Reservations.Contracts.IReservationByGuestPhoneReader` (ADR-029; Architecture Principles §14, Exceção 13) — consumida exclusivamente por Communication, retornando `IReadOnlyList<ReservationCandidate(ReservationId, PropertyId, CheckInAt, CheckOutAt)>`. Elegibilidade: apenas `ReservationStatus.Confirmed`, sem janela temporal. 0 candidatos ou N candidatos nunca resultam em `Conversation` criada — apenas exatamente 1 candidato resolve automaticamente; desambiguação entre N candidatos é deferida a um checkpoint futuro do AI Agent.

`Communication.Domain.Conversation` (novo agregado) — um por `(TenantId, ReservationId, Channel)` ativo, origem sempre `ReservationId` (Documento 12 §9/§17). `Communication.Domain.Message` ganha `ConversationId`/`Direction` (`Outbound`/`Inbound`), sem quebra de nenhum invariante pré-existente — toda mensagem anterior foi backfillada deterministicamente (`IHostPro.MigrationRunner.ConversationBackfillBootstrapStep`, mecanismo ADR-017, necessário por RLS/FORCE nas duas tabelas). Idempotência do inbound por `TenantId`/`Channel`/`ProviderMessageId`, reaproveitando o mecanismo de `IdempotencyKey` já existente — nenhuma tabela de deduplicação nova.

| Evento/Contrato | Exchange | Routing Key | Fila |
|---|---|---|---|
| GuestAccessDeliveryRequested | guest-operations-events | guest_access_delivery_requested | communication.guest-access-delivery-trigger |
| InboundGuestMessageReceived | external-integrations-events | inbound_guest_message_received | communication.inbound-guest-message-trigger |