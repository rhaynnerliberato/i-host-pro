<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Fase 9, Checkpoint 2.1 — External Integrations + Credential/Configuration
    Foundation. This is the ONLY project of External Integrations any other
    Bounded Context may reference (Architecture Principles §13, corrected by
    ADR-021 to reconcile with §17). IMessagingProvider is a plain,
    provider-neutral synchronous port.

    Fase 9, Checkpoint 2.3.3 (ADR-022 item 14) — WhatsAppMessageStatusChanged
    is this context's first Integration Event, so BuildingBlocks.Messaging.Abstractions
    is now referenced (mirrors every other Contracts project's own precedent).
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\BuildingBlocks\IHostPro.BuildingBlocks.Messaging.Abstractions\IHostPro.BuildingBlocks.Messaging.Abstractions.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
