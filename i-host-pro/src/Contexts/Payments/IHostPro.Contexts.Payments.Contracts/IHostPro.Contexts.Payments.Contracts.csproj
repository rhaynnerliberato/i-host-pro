<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Fase 10, Checkpoint 5 — PIX/Payment Deterministic Foundation. This is the
    ONLY project of Payments any other Bounded Context may reference (mirrors
    every other context's own Contracts project). PixChargeCreated/
    PixChargeConfirmed are this context's published Integration Events.
    PixChargeConfirmationReceived is a cross-context COMMAND received by
    Payments — mirrors CreateCleaningForReservation/CloseReservation living
    in the RECEIVING context's own Contracts (Architecture Principles §14
    Exception 2 precedent). IFrontDeskContactReader/IPixChargeDeliveryReader-
    style synchronous read exceptions also live here (ADR-027, Exception #11).
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\BuildingBlocks\IHostPro.BuildingBlocks.Messaging.Abstractions\IHostPro.BuildingBlocks.Messaging.Abstractions.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
