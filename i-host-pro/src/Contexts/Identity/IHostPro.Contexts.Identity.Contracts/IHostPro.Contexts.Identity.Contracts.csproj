<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Structural placeholder (Architecture Principles, Section 16): every Bounded
    Context module owns a Contracts project from creation, even before it has
    any Integration Event to publish. Identity & Access publishes its first
    events (UserCreated, UserBlocked, PermissionChanged) in a later increment
    — see the Incremento 1 report for the full event classification.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\BuildingBlocks\IHostPro.BuildingBlocks.Messaging.Abstractions\IHostPro.BuildingBlocks.Messaging.Abstractions.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
