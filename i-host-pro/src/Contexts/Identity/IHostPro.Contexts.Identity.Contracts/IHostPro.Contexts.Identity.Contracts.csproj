<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Identity & Access's first published Integration Events (Incremento 2,
    Etapa 15): UserLoggedIn, LoginFailed, AccountLockedOut, UserLoggedOut,
    RefreshTokenReuseDetected, SessionRevoked — see Documento 07 §13/§13.1
    and ADR-013 (RabbitMQ routing convention).
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\BuildingBlocks\IHostPro.BuildingBlocks.Messaging.Abstractions\IHostPro.BuildingBlocks.Messaging.Abstractions.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
