# Provisiona automaticamente o Prometheus como datasource do Grafana,
# preservando a integração descrita no ADR-007 sem passo manual na UI.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
