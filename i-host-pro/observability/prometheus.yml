# Prometheus faz scrape do endpoint exposto pelo OpenTelemetry Collector
# (não das aplicações diretamente — ver ADR-007).

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]
